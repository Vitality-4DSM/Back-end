import dashboard from '../../src/controllers/dashboard';
import { expect } from "chai";

describe('TESTE UNITÁRIO - Inicialização do controller de Dashboard', () => {
    it('Deve conter todos os atributos corretamente', () => {
        
    });
}
);